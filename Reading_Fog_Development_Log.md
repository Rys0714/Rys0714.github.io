# Reading Fog Exp.02 开发日志

## 2026.07.31｜从鼠标操作转向纯眼动交互

### 本轮问题

现有原型让鼠标同时承担“模拟视线”和“拖拽 UI”两种职责。正式接入 WebGazer 后，玩家如果还要配合键盘或鼠标组织故事，阅读过程会被额外操作打断；若直接用视线代替点击，又容易把普通扫视误判为选择。

### 已确认的设计判断

- `FOUND` 只表示“玩家已经读清过这个片段”，不是最终答案，因此自动进入后无需撤销。
- 阅读区和底部 UI 使用不同的凝视含义：阅读区负责显影与发现；底部栏负责选择、排序和修改。
- 发现条件采用同一片段累计注视 `3秒`，而不是连续死盯。
- 短暂视线偏移不会清零累计值；回看时继续累加。
- 阅读区不显示收集进度条，只在达到阈值、进入 `FOUND` 时提供轻微反馈。
- 最终故事在确认前必须允许反悔。

### 本轮已实现：纯眼动测试版（鼠标位置模拟视线）

1. 开始
   - 不点击按钮；将视线模拟点停在开始按钮约 `1.2秒` 后进入游戏。

2. 阅读与发现
   - 普通显影速度继续保持约 `500ms`。
   - 同一文字片段累计注视达到 `3秒` 后，自动进入底部 `FOUND`。
   - 视线离开后累计值暂停，回看时继续；不会重新从零开始。
   - `FOUND` 的触发不再依赖片段是否曾经完全显影。

3. 故事选择
   - 凝视 `FOUND` 卡片约 `1.5秒`，片段按选择顺序自动进入三个故事位。
   - 阅读区域与底部 UI 的判定分离；凝视底部卡片时不会继续触发雾面阅读。
   - UI 凝视使用小型圆环显示确认进度，普通阅读区不显示进度。

4. 反悔与替换
   - 凝视已填入的故事位约 `1.5秒`，该位置进入“待替换”状态。
   - 再凝视另一个 `FOUND` 片段约 `1.5秒`，完成替换。
   - 如果新片段已经在其他故事位中，两者交换位置，避免重复。
   - 再次凝视同一待替换位置可取消修改。

5. 完成
   - 三个故事位填满后，凝视“完成”约 `2秒` 生成故事卡。
   - 主流程不需要点击、拖拽或键盘。

### 同轮保留的既有机制

- 删除 `SPACE` 全局位置图。
- 停滞提示保持后台触发：约 `8秒` 无新发现出现第一层提示，`14秒` 增强，`22秒` 增加第二处；不显示倒计时，也不告诉玩家等待即可获得提示。
- 已发现片段集中保存在同屏底部 `FOUND`，避免最后依靠坐标重新寻找，使游戏退化为方位记忆。
- 普通显影窗口使用真正的边缘羽化：中心约 `72%` 完全清晰，外侧约 `28%` 渐进回雾。
- 锚点维持硬边圆框、透明内部与地图水滴图标；本轮先不决定其在最终纯眼动版本中应当手动保留还是改为自动焦点。

### 开发过程中的视觉修正记录

普通显影圈曾被误做成“浅白色圆盘加整体模糊”，导致圆心也覆盖白雾。随后改为直接在雾层上挖出羽化窗口：中心无白色填充，只有圆周附近渐进过渡。锚点与普通阅读圈也已拆为两套独立视觉样式，避免锚点跟随羽化。

### 待真人眼动设备验证

- `3秒`累计注视是否过长，是否会迫使玩家刻意停留。
- 眼动抖动情况下，阅读区与底部 UI 的边界是否稳定。
- `1.5秒`选择／替换是否既能避免误触，又不会显得拖沓。
- 圆环确认是否足够可理解，是否反而增加等待感。
- 故事位填满后，玩家能否自然理解“先凝视故事位，再凝视 FOUND 片段”的反悔顺序。
- 手动锚点是否应保留；若坚持全程纯眼动，更可能需要改为自动焦点或取消。

## 2026.08.01｜从“眼动选择协议”转向“阅读路径生成解释”

### 本轮问题

上一版虽然证明了“只用凝视也能完成选择、排序、替换和确认”，但试玩时出现了新的偏差：玩家的注意力逐渐从理解雾中的信息，转向完成一连串计时任务——盯住文字 `3秒` 收集、盯住卡片 `1.5秒` 选择、盯住按钮 `2秒` 确认。可见的秒数、圆形进度和 `发现数 / 总数` 也暗示玩家应当尽量找全片段。阅读路径虽然被记录进 JSON，却没有真正改变结局。

因此，上一版被重新定义为“成功的眼动选择协议原型”，而不是最终的“阅读路径生成意义”游戏。

### 核心判断

- 最终故事不能主要由底部 UI 的主动选卡决定，必须由玩家在阅读区中的起点、跳转、回看、停留与最后落点共同决定。
- `3秒`累计停留继续保留，但降为系统内部判断“这个片段已经被深入阅读”的隐性阈值。
- 玩家不再通过操作底栏反悔；重新阅读其他文字，就是修改解释本身。
- Explorer / Exploiter 不再被拆成两个角色或两个强制阶段：广泛扫视形成弱候选，回到少数片段反复比较会加强局部关系，玩家在一次意义形成过程中自然切换两种阅读方式。
- 驱动力应来自一个需要回答的悬念，以及“继续阅读会立即改变当前解释”，而不是数量奖励。

### 本轮已实现：阅读路径生成解释版

1. 开场悬念
   - 增加问题：“二十三时四十七分的站台上留下了一段没有结尾的记录。那晚发生了什么？”
   - 文本片段更新为围绕同一站台事件的九条歧义线索，例如“彼女は毎晩来る”“列車は止まらない”“三年前の記録”“放送が名を呼ぶ”。
   - 玩家探索的目标从“找到雾中的词”转为“对未完成记录形成解释”。

2. 隐藏操作计时
   - 开场不再显示 `3.0 s found`、`1.5 s select` 等准确秒数。
   - 删除所有圆形凝视进度环。
   - 删除 `FOUND`、三格故事位、发现数量和总数。
   - 凝视确认仅以按钮整体亮度轻微变化提供反馈，不再呈现为独立计时任务。

3. 当前解释
   - 底部 UI 改为 `CURRENT INTERPRETATION`，显示系统当前推断出的三段语义路径。
   - 第一个被深入阅读的片段获得“起点”权重。
   - 连续的视线转移建立有方向的关系；远距离快速转移记录为“飞越”；从 B 返回 A 或反复进入同一片段会加强“回看／比较”关系。
   - 停留时间、进入次数、回看次数和最近一次落点共同参与片段权重。
   - 系统枚举已深入阅读片段中的候选路径，选择关系最强的一条三段路径，并实时生成当前故事。

4. 通过阅读反悔
   - 玩家不需要凝视底栏卡片，也没有替换模式。
   - 回到雾中重新阅读其他片段，会增加新的节点和边权；当新的关系超过旧关系时，底部解释自动重排或替换。
   - 底部解释变化时使用轻微淡入反馈，强调“阅读正在改写意义”。

5. 结局
   - 当系统形成三段解释后，出现“ここで物語を止める”。凝视确认后生成故事卡。
   - 结果页显示生成的故事，以及阅读路径如何形成它：从哪一段开始、是否反复回到中间证据、最后落到哪一段。
   - 结果页追加一个未被选入主路径的替代线索，提示“如果先深入阅读它，记录可能会走向另一种意义”，但不公布唯一正确答案。
   - JSON 同步保存生成故事、关系类型、关系强度、替代线索和完整阅读事件。

### 本轮保持不变

- 雾中显影、离开回雾、回看快速恢复与显影区边缘羽化。
- 停滞提示仍按“距离上一次形成新深入阅读片段的时间”触发；本轮没有重新设计其触发条件。
- 手动锚点暂时保留原有方式；其是否改为自动焦点仍是独立待验证问题。

### 待验证

- 玩家是否能在没有秒数和进度环的情况下理解“继续看”会使解释形成。
- 三段解释的变化是否可感知，又是否会频繁跳动到难以理解。
- 重新阅读一个新片段需要多强的关系，才能自然地覆盖旧解释。
- 生成的连接词是否能准确区分顺序、回看比较和远距离跳转，而不会把眼动统计生硬地翻译成叙事语法。
- 明确悬念是否足以驱动第一次广泛探索，以及当前解释中的矛盾是否能驱动后续精读。
- 正式 WebGazer 输入下，累积停留和快速跳转的阈值是否仍然稳定。

## 2026.08.01｜底部信息层级简化

### 本轮问题

“阅读路径生成解释版”仍在底栏顶部显示 `SEARCH / FOCUS / LOOK / REVISIT / JUMP` 等实时模式，以及一条随行为变化的状态说明。它们能帮助开发者理解系统判定，却会让玩家注意到系统正在分类自己的操作，削弱雾中阅读本应具有的安静感。它们还占据了底栏最上方的一整行，使真正重要的“当前解释”和被路径选中的词仍然偏小。

### 本轮判断

- `SEARCH / LOOK` 属于内部行为标签，不是玩家形成意义所必需的信息。
- 玩家应当通过文字显影、关系变化与当前解释的更新理解系统反馈，不需要实时看见行为分类。
- 删减后的空间优先让给语义结果，而不是增加新的说明或装饰。

### 本轮已实现

- 删除底栏中的实时模式标签与状态句，包括 `SEARCH`、`FOCUS`、`LOOK`、`REVISIT`、`JUMP` 和锚点移动提示。
- 底栏只保留 `CURRENT INTERPRETATION`、悬念、当前三段语义关系和结束确认。
- 放大 `CURRENT INTERPRETATION` 与悬念文字，强化信息层级。
- 将选中词卡的最小宽度、内边距和词语字号一并提高；关系箭头、关系说明与实时生成的故事句也同步小幅放大。
- 不新增图标、色块或说明来填补留白，让删减真正转化为安静和可读性。

### 待验证

- 玩家是否仍能仅通过解释变化理解“重新阅读会改写意义”。
- 放大的三张词卡在常用桌面宽度下是否仍能完整显示故事预览与结束按钮。
- 正式眼动输入时，取消行为标签是否让注意力更集中于雾中线索，而不是降低系统反馈的可理解性。
