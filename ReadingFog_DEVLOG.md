# Reading Fog 开发日志

> 本文根据原型文件、试玩反馈与开发对话，于 2026-07-29 回溯整理。  
> 早期版本没有逐日记录，因此 v0.1～v0.3b-core 主要记录设计变化；从 v0.3b.1 起按实际版本持续更新。

## 项目方向

Reading Fog 是一个关于阅读差异与信息碎片化的互动原型。

它不把停顿、折返、跳读或碎片化理解视为错误，也不要求玩家还原唯一的“正确句子”。系统希望记录并显现：

- 玩家先注意到什么；
- 玩家选择保留什么；
- 玩家如何在片段之间建立关系；
- 不同选择顺序如何形成不同的阅读结果。

当前版本以鼠标位置模拟视线，未来再接入眼动追踪，并从单人原型扩展到双人协作。

---

## v0.1｜双人机制草案

### 想验证什么

最初尝试把两种阅读倾向拆成两种角色：

- **Explorer**：快速探索、跳读、发现可能性；
- **Exploiter**：关注局部、维持信息、把线索具体化。

两名玩家掌握不完整的信息，需要通过交流共同重建文字意义。

### 主要机制

- 迷雾中的文字达到阈值后显影；
- 阅读路径短暂保留，随后衰减；
- 玩家设置记忆锚点并连接路径；
- 答错后回到前一个锚点；
- 第二名玩家通过阻力条、按压或翻页装置影响阅读过程；
- 曾考虑加入误导按压、雾回弹等合作与对抗行为。

### 发现的问题

- 双人、实体装置、迷雾、锚点、对抗等机制同时出现，变量过多；
- 还没有证明单次阅读互动本身是否有趣；
- 玩家容易把目标理解成“找出系统预设答案”。

### 决定

暂时延后双人和实体装置，先制作能够独立试玩的单人版本，验证最小核心循环。

---

## v0.2｜单人迷雾与锚点

### 想验证什么

把阅读中的“发现—遗忘—标记—连接”转化为可操作的游戏循环。

### 核心循环

1. 用鼠标模拟注视，在迷雾中探索；
2. 文字与短时轨迹显现；
3. 信息重新模糊；
4. 设置有限的记忆锚点；
5. 按顺序连接锚点；
6. 重建语义并消散迷雾。

第一关使用 8 个文字碎片，其中包含 5 个关键信息与 3 个干扰信息。

### 试玩后发现的问题

- 玩家可以逐一搜索所有位置，形成 brute force 式试错；
- 停留读条只制造等待，没有产生阅读意义；
- 回声、拖尾与文字之间的关系不清楚；
- 圆形显影叠加后容易卡顿；
- 以“关键锚点＋正确语序”作为通关条件，仍然在验证标准答案；
- 连线完成后缺少结果反馈，玩家容易“连完以后尬住”。

### 保留

- 雾中寻找文字；
- 有限选择；
- 弱提示式的“躲猫猫”正向反馈；
- 选择顺序可以成为阅读记录。

### 删除或调整

- 删除读条，文字改为约 100ms 淡入；
- 不再用等待时间制造难度；
- 尝试“标记三点＋连线排序”；
- 开始探索开放式结果，而不是只有正确语序；
- 暂时继续使用鼠标，眼动追踪留到核心玩法稳定之后。

---

## v0.3a｜开放组合实验

### 想验证什么

从“重建正确语义”转向“玩家用有限片段形成自己的解释”。

### 发现的问题

- 结尾要求玩家选择关系、解释组合或输入文字，体验接近问卷；
- 玩家完成操作后没有明显的世界变化或惊喜；
- 系统虽然允许开放组合，但仍要求玩家在游戏外说明自己的理解；
- 操作规则的存在感强于阅读本身。

### 决定

- 删除关系词；
- 删除手动解释与文字输入；
- 删除结尾问卷；
- 让玩家的理解直接通过声音、光线、场景与时间变化被系统呈现。

---

## v0.3b-core｜车站与文字回声

### 想验证什么

用统一的“深夜车站”世界承接文字、声音与视觉反馈，让玩家感到文字正在逐步恢复一个场景。

### 主要改动

- 语料改为日语短片段；
- 鼠标移动模拟注视；
- `SPACE` 确认文字或扩展阅读单位；
- 确认文字后，车站与中央时钟逐渐显现；
- 游玩过程中隐藏探索轨迹，结局时再短暂回放；
- 设置三组“回声组合”，组成其中一组后自动进入结局；
- 不再要求玩家回答问题或输入解释。

### 有效的部分

- 整体美术风格成立；
- 文字被选中后逐渐显现的效果具有辨识度；
- 随着选择增加，中央时钟越来越清楚，形成了视觉目标；
- 日语短片段与车站环境能够互相强化。

### 试玩反馈

- 点击音只是“操作成功音”，没有世界观内的因果；
- 搜索负担较高，需要非常有耐心的玩家；
- 必须找到预设组合才能通关，本质上仍是隐藏标准答案；
- 中央时钟只是伪装成场景元素的进度条，功能不明确。

### 决定

保留现有美术、阅读窗口与文字显现效果，重做声音逻辑、结束条件和中央时钟的功能。

---

## v0.3b.1｜Your Station

**状态：已实现并发布**

试玩链接：<https://reading-fog-v03b1.therain-w.chatgpt.site>

### 本版目标

取消唯一正确组合，让系统判断“玩家是否形成并主动结束了一次阅读”，而不是判断理解是否符合作者答案。

### 已实现

- 阅读窗口大小保持不变；
- 看过但未确认的文字不会留下永久残影；
- 已确认文字保持清晰；
- 每个有效蓝点对应一次当前选择；
- 第三个蓝点出现时，中央时钟立即开放；
- 将鼠标停在中央时钟上并长按 `SPACE`，固定当前选择并形成车站；
- 不同选择与顺序可以产生不同的场景结果；
- 所有结果地位相同，不显示正确、错误或最好结局；
- 确认音改为时钟齿轮与机械恢复声；
- 环境声只在相应场景形成后出现；
- 玩家长时间没有发现文字时，只出现短暂的呼吸提示；
- 游玩过程中继续隐藏探索轨迹。

### 中央时钟的功能

中央时钟不再只是进度条，而承担三个功能：

1. **目标**：让停止的时钟重新运转；
2. **记录**：随着玩家确认片段而逐渐显现；
3. **结束操作**：玩家主动长按 `SPACE`，固定自己的阅读版本。

### Bug 修复记录

#### 1. 已确认文字与蓝点数量不一致

**现象：** 场景中已经点亮 5 个文字，但只显示 3 个蓝点。  
**原因：** 扩展阅读单位时只更新了文字状态，没有同步生成蓝点。  
**修复：** 每次有效确认都同步留下蓝点；撤销后对应标记变为淡色残影。

#### 2. 三个蓝点出现后时钟仍未开放

**现象：** 玩家确认「最終＋列車＋時計」并看到 3 个蓝点，但顶部只记录 2 格。  
**原因：** 程序仍在后台把「最終＋列車」合并为一个词组，画面与内部计数采用了不同规则。  
**修复：** 顶部三格、中央时钟开放与结束条件全部统一使用“当前有效蓝点数量”。第三个蓝点出现的同一刻，时钟立即开放；撤销到两个蓝点后重新关闭。

### 本版暴露出的新问题

取消标准答案后，玩家也可以不理解内容，找到任意三个片段便结束：

> “没有标准答案”可能变成“随便选三个也一样”。

这说明下一步不应恢复正确答案，而应让片段之间的关系在场景中产生可感知的差异。

---

## v0.3b.2｜词语共鸣

**日期：2026-07-29**  
**状态：已实现并发布**

试玩链接：<https://reading-fog-v03b2.therain-w.chatgpt.site>

### 核心原则

> 碎片不是错误，而是阅读发生的起点；组合也不是寻找答案，而是在碎片之间建立关系。

随机选择三个片段仍然可以结束，形成较为破碎的场景；认真阅读并组合相关片段，会形成更明确的关系。两者都不是失败，系统不评分，也不标记“完整／残缺”。

### 本版想验证什么

在没有标准答案的前提下，让玩家的选择仍然具有意义：

1. 单个片段可以独立成立；
2. 两个片段之间可以产生关系；
3. 多个关系可以共同改变同一个车站；
4. 系统呈现关系如何形成，但不评价关系越多就越正确。

### 已实现

- 单独确认一个片段时，只出现微弱、局部的场景变化；
- 新片段与已有片段产生关系时，光、雨、人物、雨伞或时钟开始互相影响；
- 多个关系同时成立时，轨道、站台与环境光的变化变得更连续；
- 关系密度只通过场景表现，不显示分数、等级或“完整度”；
- 每一次有效确认仍对应一个蓝点；
- 第三个蓝点出现的同一刻，中央时钟立即开放；
- 玩家可以在三个片段后结束，也可以继续寻找更多片段；
- 同样的片段以不同顺序选择时，场景焦点、时钟时间、结局标题与结尾文字会变化；
- 结局按实际选择顺序显示片段，而不是在后台重新合并成词组；
- 共鸣发生时，在原有时钟机械声上叠加不同层次的和声反馈；
- 所有结局地位相同，不显示正确、错误、完整、残缺或最佳结果。

### 当前关系设计

| 片段关系 | 场景变化 |
| --- | --- |
| 列车 × 延迟 | 远处灯光接近后停在雾中 |
| 列车 × 人影 | 等待的人转向轨道或远处灯光 |
| 列车 × 时钟 | 轨道声与时钟产生共同节奏 |
| 延迟 × 人影 | 人影停留，雨与等待时间被拉长 |
| 延迟 × 时钟 | 时钟表现出停滞与被推回的感觉 |
| 雨伞 × 人影 | 红色雨伞移动到等待的人影附近 |

同一对关系还会根据先后顺序产生不同文本。例如：

- `列車 → 誰か`：人影被列车的气配吸引，转向轨道；
- `誰か → 列車`：先出现等待的人，之后其前方才出现灯光。

### 与 v0.3b.1 相比保留的部分

- 阅读窗口大小不变；
- 看过但未选择的文字不留下永久残影；
- 已选择文字始终清晰；
- 游玩过程中不显示探索轨迹；
- 中央时钟继续作为目标、记录和主动结束装置；
- 不恢复唯一正确组合；
- 不增加文字数量。

### Bug 修复

#### 撤销后底部仍显示 `HOLD`

**现象：** 三个蓝点开放时钟后，玩家把鼠标停在时钟上再撤销，底部状态偶尔仍显示 `HOLD`。  
**原因：** 时钟是否开放与鼠标是否停在时钟上是两个状态，撤销只改变了前者。  
**修复：** 底部提示必须同时满足“当前至少三个有效蓝点”与“鼠标位于时钟上”才显示 `HOLD`。

### 下一轮需要验证

- 玩家能否在没有长说明的情况下理解“片段会互相回应”；
- 破碎场景是否会被感知为一种成立的结果，而不是未完成品；
- 如何呈现关系密度，同时不暗示“关系越多越正确”；
- 中央时钟的长按结束操作是否足够直观；
- 玩家是否会因为场景变化而重新回看之前发现的文字；
- 六组关系是否足够可感知，还是有些反馈过于隐晦；
- 选择顺序造成的差异是否能被玩家注意到。

---

## v0.3b.3｜Keep or Replace

**日期：2026-07-29**  
**状态：已实现并发布**

试玩链接：<https://reading-fog-v03b3.therain-w.chatgpt.site>

### 本版想验证什么

把“选满三个片段之后的选择”变成真正的核心玩法：

> 玩家可以接受当前保留的信息，也可以为了新发现，主动舍弃并重组已有信息。

这次不再把第三个片段当成自动通关条件，也不继续用场景物件包装结束按钮。玩家需要自己决定何时停止寻找，以及什么值得留下。

### 为什么删除中央时钟

试玩发现，中央时钟虽然会随片段与顺序变化，但玩家无法从词义推测时间，也无法通过第二次游玩总结规律。

- 每个词组只是在后台对应一个隐藏数字；
- 选择顺序经过加权后生成一个装饰性时间；
- 任意三个词都会让指针变化，但变化与词义没有可理解的对应；
- 长按结束后，时钟没有真正开始走，只是雾变淡、场景变清楚并出现结局。

因此，时钟实际上同时承担了计数器、结算按钮和随机外观三个功能，却没有形成可感知的因果关系。“让停止的世界重新流动”是开发者后来补上的解释，不是玩家实际体验到的机制。

### 已删除

- 中央时钟图形与指针变化；
- 词语到时间的隐藏数字映射；
- 三个蓝点后开放时钟；
- 注视中央时钟并长按 `SPACE`；
- `HOLD` 状态与长按进度环；
- 所有“让时钟重新开始”的操作提示；
- 结局卡片中的随机时间。

文字片段「時計は止まった」仍作为车站语料保留。被删除的是时钟作为核心操作与结算装置，而不是语料本身。

### 新核心循环

1. 在雾中探索文字；
2. 用 `SPACE` 保留片段；
3. 最多保留三个片段；
4. 三个片段后，可以直接选择「この読みを残す」；
5. 如果发现新的片段，先把它设为候选；
6. 再回到三个已保留片段中，主动选择一个舍弃；
7. 新片段替换旧片段，场景关系随之重组；
8. 玩家决定不再寻找时，固定当前阅读结果。

### 已实现

- 顶部三格从「時計」改为「残した断片」；
- 第三个片段出现后，直接开放「この読みを残す」；
- `ENTER` 可以固定当前结果，不再需要长按；
- 满三个片段后发现新词，会进入候选状态；
- 候选词以不同颜色保持清晰；
- 玩家必须回到一个旧蓝色片段上按 `SPACE`，明确决定舍弃什么；
- 候选未处理时不能结束，`ESC` 可以把候选退回雾中；
- 替换完成后始终仍只保留三个片段；
- 场景关系、选择顺序与结局文本根据替换后的三个片段重新计算；
- 结局只显示 `YOUR STATION`，不再生成没有语义依据的时间。

### 本版的研究意义

这一版开始显现的不只是“玩家选择了什么”，还包括：

- 获得三个碎片后是否立即停止；
- 是否为了更明确的关系继续寻找；
- 是否舍不得删除最初看到的片段；
- 新信息出现时，玩家会保留、舍弃还是推翻原来的理解；
- 阅读结果如何在有限记忆容量中被重新组织。

随机选择三个片段依然可以成立并结束；替换也不代表更正确。系统只把玩家如何处理有限信息的过程变成可操作的行为。

### 实际验证

已完整测试以下流程：

1. 选择三个片段；
2. 发现第四个片段并将其设为候选；
3. 指定一个旧片段进行替换；
4. 确认顶部仍保持三个片段；
5. 直接固定当前阅读；
6. 结局按替换后的三个片段生成。

### 下一轮需要验证

- 玩家是否能在没有额外说明时理解“先选新词，再选择要舍弃的旧词”；
- 主动舍弃是否形成有意义的犹豫，而不是增加操作负担；
- 有多少玩家会在第一次凑满三个片段后立即结束；
- 替换前后的场景变化是否足以促使玩家重新思考组合；
- 固定三个片段是否太少，还是刚好能够制造信息取舍；
- 未来双人版中，两名玩家对“保留谁、舍弃谁”的分歧能否成为协作讨论。

---

## v0.3c｜An Unresolved Question

**日期：2026-07-30**  
**状态：已发布**  
**试玩链接：<https://reading-fog-v03c.therain-w.chatgpt.site>**

### 本版想验证什么

v0.3b.3 已经能够记录玩家对三个片段的保留、舍弃与替换，但仍缺少一个关键条件：

> 玩家为什么要在这些片段之间取舍？

如果没有需要回应的问题，玩法很容易缩成“挑三个觉得有意思的词”，替换的理由也可能只是想看画面发生什么变化。本版要验证的是：持续存在的未解问题，是否能让玩家把新片段理解为对现有解释的支持、补充或推翻。

本版使用的问题为：

> **終電のあと、なぜ誰かはホームに残っていたのか。**  
> 末班车之后，为什么还有人留在站台？

### 核心循环

> **明确问题 → 雾中发现碎片 → 保留三个 → 替换或坚持 → 留下当前解释**

问题只提供阅读方向，不提供隐藏答案。随机保留三个片段仍然可以结束；系统不会根据片段之间的关系给分，也不会把更连贯的组合判为更正确。

### 已实现

- 开场直接呈现未解问题；
- 探索过程中，问题始终显示在阅读区域上方；
- 保留 v0.3b.3 的三片段容量、候选词与主动舍弃操作；
- 三个片段后仍可直接结束，不强制替换；
- 替换后的新片段占据旧片段原来的位置，保留“理解被修改”的顺序关系；
- 结局重新呈现同一个问题；
- 结局按顺序显示最终留下的三个片段；
- 每次替换以 `旧片段 → 新片段` 的形式记录；
- 没有发生替换时，也以同样条件结束；
- 删除系统自动补写的 `YOUR STATION` 官方故事与结局标题；
- 不增加时钟、翻页、分数、倒计时、关系完成度或手动输入解释。

### 为什么删除官方故事

此前结局会根据后台关系表自动生成一个较完整的车站叙事。它虽然能增强气氛，却容易替玩家完成解释，使研究对象从“玩家如何组织有限信息”变成“系统如何替选择配故事”。

v0.3c 只保留可观察的阅读结果：

1. 玩家面对的问题；
2. 最后保留的三个片段；
3. 片段的排列顺序；
4. 哪些片段曾被舍弃和替换。

系统显现理解的变化，但不宣告玩家最终理解了什么。

### 实际验证

已完整测试以下流程：

1. 保留 `最終 → 列車 → 誰か`；
2. 找到第四个片段 `止まった`；
3. 将 `止まった` 设为候选；
4. 主动舍弃 `最終`；
5. 最终结果保持为三个片段：`止まった → 列車 → 誰か`；
6. 结局正确显示替换轨迹：`最終 → 止まった`；
7. 未发现界面或交互脚本错误。

### 当前风险

- “为什么”这一问可能把开放阅读收窄成寻找因果答案；
- 玩家可能根据问题直接搜索「誰か」「待っている」，减少对其他片段的探索；
- 替换轨迹能证明理解发生变化，但还不能证明变化是由问题驱动；
- 单局内的选择行为仍不足以区分“认真组织”与“随便尝试”。

### 下一轮需要验证

- 玩家替换片段时，是否会说出“这个更能回应问题”一类理由；
- 与 v0.3b.3 相比，玩家是否更愿意重新检查已经保留的三个片段；
- 问题是否提供了必要方向，还是过度限制了碎片的开放解释；
- 没有替换的玩家，是已经形成解释，还是没有理解替换操作；
- 结尾的替换轨迹是否能帮助玩家回想自己改变理解的时刻；
- 未来双人版中，同一个问题能否成为两个人讨论保留与舍弃的共同依据。

---

## v0.3d｜Three Fragments Remain

**日期：2026-07-30**  
**状态：已发布**  
**试玩链接：<https://reading-fog-v03d.therain-w.chatgpt.site>**

### 本版想验证什么

v0.3c 用未解问题为片段取舍提供理由，但实际试玩暴露出新的偏移：

> 「为什么有人留在站台？」会让玩家寻找证据、组合答案并提交结果，结构上仍然接近阅读理解题。

本版要验证的是：不规定玩家应该理解什么，只用“信息会消失”和“三个片段容量”能否自然产生保留、舍弃、折返与重组。

### 核心循环

> **短暂发现 → 留下片段 → 达到三个 → 继续寻找 → 替换或坚持 → 固定结果**

玩家仍有明确的行动条件，但没有语义问题：

> **见过的词会重新回到雾中，只能留下三个。**

随机留下三个、追求明确关系、保留强烈但断裂的印象都可以结束。系统不要求玩家说明理由，也不判断哪一种阅读更完整。

### 已实现

- 删除开场的未解问题；
- 删除探索过程中始终悬浮的 `QUESTION`；
- 删除结果页的问题回顾；
- 将开场改为“见过的词会回到雾中，只能留下三个”；
- 将按钮从「この読みを残す」改为「この三つを残す」；
- 将结果标题从「あなたが残した読み」改为「あなたが残した三つ」；
- 保留三片段容量、第四个候选和主动舍弃操作；
- 保留 `旧片段 → 新片段` 的替换轨迹；
- 保留单个片段与片段关系产生的场景反馈；
- 不加入新装置、翻页、分数、倒计时或正确答案。

### 为什么不是简单退回 v0.3b.3

v0.3c 的问题被删除，但“理解如何变化”的记录被保留下来。v0.3d 因此不是回到只有三词结算的选词器，而是继续显现：

- 最初注意到并保留了什么；
- 三个位置满后是否继续搜索；
- 新片段出现时是否舍得放弃旧片段；
- 哪个旧片段被哪个新片段替代；
- 玩家在什么时候主动停止寻找。

取舍动机从“回答外部问题”转为“抵抗信息消失，并管理有限容量”。

### 实际验证

已完整测试以下流程：

1. 保留 `最終 → 列車 → 誰か`；
2. 找到第四个片段 `止まった`；
3. 将 `止まった` 设为候选；
4. 主动舍弃 `最終`；
5. 最终结果保持为三个片段：`止まった → 列車 → 誰か`；
6. 结尾正确显示替换轨迹：`最終 → 止まった`；
7. 开场、探索和结果页面均不再出现问题或答案式语言；
8. 桌面端完整流程与结尾动画正常。

### 本版判断

“问题”虽然能让玩家有明确方向，却会预先规定一种因果式阅读。v0.3d 把目标降低到行动层：

> **不是回答车站发生了什么，而是决定哪些短暂出现的信息值得留下。**

这更接近最初要显现的阅读过程，而不是把碎片做成题目的证据。

### 下一轮需要验证

- 没有未解问题后，玩家是否仍然愿意在满三个后继续探索；
- 玩家替换片段时，是基于词义关系、视觉印象、最初记忆还是其他理由；
- “只能留下三个”是否足以制造真实犹豫；
- 玩家是否能在没有额外说明时理解候选与主动舍弃操作；
- 结尾替换轨迹是否能帮助玩家回想自己的注意和判断变化；
- 不替换的玩家是在坚持最初印象，还是没有发现更多片段；
- 单人循环是否已经足够稳定，可以开始验证两个人如何协商保留与舍弃。

---

## 双人版本方向

v0.3d 之后，单人版的最小互动可以概括为：

> **发现片段 → 保留三个 → 新发现 → 舍弃或维持 → 固定结果**

未来双人版可以把“提出、回应与取舍”拆给两名玩家：

1. 玩家 A 在自己的雾中发现并提出一个片段；
2. 玩家 B 从自己掌握的另一部分文字中选择回应；
3. 两个片段共同改变车站；
4. 下一轮交换提出者与回应者；
5. 共同记忆区最多只保留三个片段；
6. 第四个候选出现后，两名玩家必须讨论要保留新片段，还是维持原来的三个；
7. 如果保留新片段，还要共同决定舍弃哪个旧片段；
8. 两名玩家共同确认并固定他们形成的车站。

两名玩家不固定对应“DD 玩家”与“一般玩家”。Explorer / Exploiter 是可交换的阅读角色，用于观察不同阅读方式如何互补，而不是给玩家贴标签。

---

## 当前锁定的设计决定

以下内容目前暂不改动：

- 阅读窗口大小保持 v0.3b.1 的状态；
- 看过但未选择的文字不留下永久残影；
- 已选择文字始终清晰；
- 不设置需要回答的语义问题，只提供“三个片段容量”这一行动条件；
- 最多保留三个有效片段；
- 第三个片段后可以直接结束，也可以用新发现替换旧片段；
- 结束使用明确的「この三つを残す」，不再包装成场景物件；
- 结局只呈现最终片段与替换轨迹，不替玩家补写完整故事；
- 不恢复唯一正确组合；
- 不加入分数、正确率或结局排名；
- 游玩过程中不显示探索轨迹；
- 声音必须由车站或文字关系产生；
- 不要求玩家选择抽象关系词或手动解释自己的理解；
- 翻页与跨页信息继承暂不考虑；
- 继续以鼠标模拟视线，待核心循环稳定后再接入眼动追踪。

---

## 后续日志模板

今后的每次更新按以下结构记录：

```markdown
## vX.X｜版本名称

**日期：YYYY-MM-DD**
**状态：设计中 / 测试中 / 已发布**

### 想验证什么

### 做了哪些改动

### 试玩反馈

### 保留、删除与原因

### Bug 修复

### 下一版要验证什么
```
